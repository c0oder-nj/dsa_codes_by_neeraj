#include<bits/stdc++.h>
using namespace std;

int main(){
    string s;
    // s = cin.get(); // it will take only 1 character
    // for taking string with spaces as input we need to use getline() or gets() method 
    cout<<s<<endl;
    return 0;
}